

这几个里，真正高频的是：

```text
802.3  以太网
802.11 无线局域网 Wi-Fi
802.1  VLAN / 交换 / 桥接 / 认证
```

其余：

```text
802.2  了解 LLC
802.4  了解令牌总线
802.5  了解令牌环
802.15 了解蓝牙 / ZigBee / 物联网
802.16 了解 WiMAX / 无线城域网
```

---

# 0. 先记住 IEEE 802 是干什么的

## 1. IEEE 802 的位置

**IEEE 802 是局域网和城域网标准族。**

它主要对应 OSI 七层模型中的：

```text
第 1 层：物理层
第 2 层：数据链路层
```

也就是说，IEEE 802 主要规定：

- 网线怎么传输数据
    
- 无线怎么传输数据
    
- MAC 地址怎么使用
    
- 以太网帧怎么封装
    
- 交换机怎么转发
    
- VLAN 怎么划分
    
- Wi-Fi 怎么通信
    

---

## 2. 数据链路层怎么拆

IEEE 802 把数据链路层又分成两部分：

```text
数据链路层
├── LLC：逻辑链路控制
└── MAC：介质访问控制
```

|子层|中文|作用|
|---|---|---|
|LLC|逻辑链路控制|给网络层提供统一接口|
|MAC|介质访问控制|控制谁能使用传输介质，处理 MAC 地址、帧传输|

考试里经常这样考：

> IEEE 802 标准主要涉及 OSI 模型的哪几层？

答案：

```text
物理层和数据链路层
```

---

# 1. IEEE 802：局域网 / 城域网标准族

## 1.1 它是什么？

**IEEE 802 是一个标准族，不是某一个单独协议。**

它包含很多子标准，例如：

|标准|对应内容|
|---|---|
|IEEE 802.1|VLAN、桥接、交换、安全认证|
|IEEE 802.2|LLC 逻辑链路控制|
|IEEE 802.3|以太网|
|IEEE 802.11|Wi-Fi|
|IEEE 802.15|蓝牙、ZigBee、WPAN|
|IEEE 802.16|WiMAX、无线城域网|

---

## 1.2 央国企常考点

### 考点 1：IEEE 802 属于哪类标准？

答：

```text
局域网和城域网标准
```

### 考点 2：IEEE 802 主要对应 OSI 哪几层？

答：

```text
物理层和数据链路层
```

### 考点 3：IEEE 802 中最常见的两个标准？

答：

```text
IEEE 802.3：以太网
IEEE 802.11：无线局域网
```

---

## 1.3 记忆方法

```text
802 是网络底层大类，
802.3 管有线，
802.11 管无线。
```

---

# 2. IEEE 802.1：桥接、VLAN、网络管理

## 2.1 它是什么？

**IEEE 802.1 主要和交换机、桥接、VLAN、生成树、安全认证有关。**

这个标准族在实际运维、网络工程、企业内网中非常重要。

你以后在公司里碰到的这些东西，很多都和 802.1 有关：

- VLAN
    
- Trunk
    
- Access 端口
    
- 交换机桥接
    
- STP 生成树
    
- 端口认证
    
- 链路聚合
    
- QoS 优先级
    

---

## 2.2 重点一：VLAN

VLAN 全称：

```text
Virtual Local Area Network
虚拟局域网
```

它的作用是：

> **把一台或多台交换机上的设备，逻辑上划分成多个不同的局域网。**

比如公司里：

```text
VLAN 10：行政部门
VLAN 20：研发部门
VLAN 30：财务部门
VLAN 40：服务器区域
VLAN 50：访客 Wi-Fi
```

虽然这些设备可能接在同一台交换机上，但通过 VLAN 可以隔离广播域。

---

## 2.3 VLAN 为什么重要？

因为默认情况下，交换机是二层设备，同一个交换机里的设备都在同一个广播域。

如果不划分 VLAN，会有几个问题：

|问题|说明|
|---|---|
|广播太多|ARP、DHCP 等广播会影响整个网络|
|安全性差|财务部和访客网络可能互相访问|
|管理混乱|不同部门、不同业务无法隔离|
|故障影响大|一个区域异常可能影响整个局域网|

使用 VLAN 后：

```text
不同 VLAN 之间默认不能直接二层通信。
```

不同 VLAN 要通信，一般需要：

```text
三层交换机 / 路由器
```

这叫：

```text
VLAN 间路由
```

---

## 2.4 重点二：IEEE 802.1Q

**802.1Q 是 VLAN 标记标准。**

它的核心作用是：

> **在以太网帧中插入 VLAN 标签，用来标识这个数据帧属于哪个 VLAN。**

802.1Q 会在以太网帧中加入一个 VLAN Tag，里面有 VLAN ID。

VLAN ID 范围常见记法：

```text
1 ~ 4094
```

其中：

```text
VLAN 1 通常是默认 VLAN
```

---

## 2.5 Access 口和 Trunk 口

这个很容易在面试和实操中被问到。

### Access 口

Access 口通常连接终端设备：

```text
电脑
打印机
摄像头
普通服务器
```

特点：

```text
一个 Access 口通常只属于一个 VLAN
```

例如：

```text
交换机端口 G0/1 设置为 access vlan 10
```

那么接到这个端口的电脑就属于 VLAN 10。

---

### Trunk 口

Trunk 口通常连接：

```text
交换机和交换机
交换机和路由器
交换机和防火墙
交换机和 AP
```

特点：

```text
一个 Trunk 口可以通过多个 VLAN 的数据
```

例如：

```text
交换机 A 和交换机 B 之间用 Trunk 连接
允许 VLAN 10、20、30 通过
```

这样两台交换机上的 VLAN 10 用户仍然可以处于同一个 VLAN。

---

## 2.6 重点三：生成树 STP

STP 全称：

```text
Spanning Tree Protocol
生成树协议
```

它主要用来解决：

```text
交换机二层环路问题
```

### 什么是二层环路？

如果多台交换机之间接成了环：

```text
交换机 A —— 交换机 B
   |              |
交换机 D —— 交换机 C
```

广播帧可能会在环里一直转发，导致：

- 广播风暴
    
- MAC 地址表震荡
    
- 网络瘫痪
    
- 交换机 CPU 飙高
    

STP 的作用是：

> **逻辑上阻塞某些端口，避免二层环路。**

物理上线路还在，但逻辑上不让某些链路转发数据。

---

## 2.7 重点四：802.1X 端口认证

**802.1X 是端口访问控制标准。**

它常用于企业内网认证。

比如你把电脑接入公司网口后，不是马上就能上网，而是需要：

```text
账号密码认证
证书认证
准入系统认证
```

认证通过后，交换机端口才允许你访问网络。

常见场景：

- 公司办公网准入
    
- 校园网认证
    
- 无线认证
    
- 终端安全检查
    

---

## 2.8 央国企考法

### 选择题常考

> VLAN 标准通常对应哪个 IEEE 标准？

答案：

```text
IEEE 802.1Q
```

---

> 生成树协议主要用于解决什么问题？

答案：

```text
二层网络环路
```

---

> 802.1X 主要用于什么？

答案：

```text
端口访问控制 / 网络接入认证
```

---

## 2.9 802.1 记忆总结

```text
802.1 管交换管理：
VLAN、桥接、生成树、认证。
```

必背：

```text
802.1Q = VLAN
802.1X = 端口认证
STP = 防止二层环路
```

---

# 3. IEEE 802.2：LLC 逻辑链路控制

## 3.1 它是什么？

**IEEE 802.2 是 LLC 逻辑链路控制标准。**

LLC 位于数据链路层的上半部分。

结构如下：

```text
数据链路层
├── LLC：逻辑链路控制，IEEE 802.2
└── MAC：介质访问控制，比如 802.3、802.11
```

---

## 3.2 LLC 的作用

LLC 主要作用是：

> **为网络层提供统一接口，屏蔽不同 MAC 技术的差异。**

也就是说，不管下面是：

```text
以太网 802.3
无线局域网 802.11
令牌环 802.5
```

LLC 都可以向上提供相对统一的服务。

---

## 3.3 这个标准现在重要吗？

在央国企笔试中，802.2 一般不会深挖。

常考程度：

```text
了解即可
```

最常见考法就是：

> IEEE 802.2 对应什么？

答案：

```text
LLC 逻辑链路控制
```

---

## 3.4 容易混淆点

|标准|内容|
|---|---|
|IEEE 802.2|LLC，逻辑链路控制|
|IEEE 802.3|MAC + 物理层，以太网|
|IEEE 802.11|MAC + 物理层，无线局域网|

你可以理解成：

```text
802.2 在上面，802.3 / 802.11 在下面。
```

---

## 3.5 802.2 记忆总结

```text
802.2 = LLC = 数据链路层上半部分
```

---

# 4. IEEE 802.3：Ethernet 以太网

## 4.1 它是什么？

**IEEE 802.3 是以太网标准。**

这是计算机网络里最重要的 IEEE 标准之一。

关键词：

```text
以太网
有线局域网
网线
交换机
MAC 地址
以太网帧
CSMA/CD
```

考试看到：

```text
IEEE 802.3
```

直接想到：

```text
Ethernet 以太网
```

---

## 4.2 以太网是什么？

以太网是目前最常见的有线局域网技术。

常见场景：

- 电脑插网线上网
    
- 服务器接入交换机
    
- 公司办公网络
    
- 数据中心网络
    
- 校园网有线接入
    
- 家用路由器 LAN 口
    

---

## 4.3 重点一：MAC 地址

以太网使用 MAC 地址在局域网内识别设备。

MAC 地址长度：

```text
48 位
```

通常写成 6 个十六进制数：

```text
00-1A-2B-3C-4D-5E
```

或者：

```text
00:1A:2B:3C:4D:5E
```

### MAC 地址属于哪一层？

答案：

```text
数据链路层
```

### IP 地址属于哪一层？

答案：

```text
网络层
```

### 常考区别

|地址|层次|作用|
|---|---|---|
|MAC 地址|数据链路层|局域网内部找设备|
|IP 地址|网络层|跨网络寻址|
|端口号|传输层|区分具体应用进程|

---

## 4.4 重点二：以太网帧格式

以太网传输的数据单位叫：

```text
以太网帧
```

典型字段包括：

```text
目的 MAC 地址
源 MAC 地址
类型 / 长度
数据
FCS 校验
```

简化结构：

```text
目的 MAC | 源 MAC | 类型 | 数据 | FCS
```

### FCS 是什么？

FCS 全称：

```text
Frame Check Sequence
帧校验序列
```

作用：

```text
差错检测
```

如果接收方发现 FCS 校验不对，说明帧可能出错，一般会丢弃。

---

## 4.5 重点三：CSMA/CD

CSMA/CD 全称：

```text
Carrier Sense Multiple Access with Collision Detection
载波监听多路访问 / 冲突检测
```

它是早期共享式以太网中的介质访问控制方法。

### 工作过程

简单理解：

```text
先听后发，边发边听，冲突停发，随机重发。
```

具体步骤：

1. 发送前先监听信道是否空闲；
    
2. 如果空闲，就发送数据；
    
3. 如果多个设备同时发送，会发生冲突；
    
4. 检测到冲突后停止发送；
    
5. 等待随机时间后重新发送。
    

---

## 4.6 现在还用 CSMA/CD 吗？

现在公司网络大多使用交换机，并且是全双工通信。

所以：

```text
现代交换式全双工以太网基本不再使用 CSMA/CD。
```

但是考试仍然会考：

```text
传统以太网使用 CSMA/CD
```

注意区分：

|网络|介质访问方式|
|---|---|
|传统有线以太网|CSMA/CD|
|无线局域网 Wi-Fi|CSMA/CA|

这个非常常考。

---

## 4.7 重点四：双工模式

以太网中有两种常见双工方式：

|模式|含义|
|---|---|
|半双工|同一时间只能发送或接收|
|全双工|可以同时发送和接收|

早期集线器 Hub 网络多是半双工，容易冲突。

现代交换机端口通常是全双工，不容易发生冲突。

---

## 4.8 重点五：冲突域和广播域

### 冲突域

冲突域是指：

> **同一时间如果多个设备发送数据，可能发生冲突的范围。**

Hub 会扩大冲突域。

交换机可以隔离冲突域。

---

### 广播域

广播域是指：

> **广播帧能够到达的范围。**

交换机默认不隔离广播域。

VLAN 可以隔离广播域。

路由器也可以隔离广播域。

---

## 4.9 Hub、交换机、路由器区别

|设备|层次|根据什么转发|是否隔离冲突域|是否隔离广播域|
|---|---|---|---|---|
|Hub 集线器|物理层|不判断，直接转发|否|否|
|Switch 交换机|数据链路层|MAC 地址|是|默认否|
|Router 路由器|网络层|IP 地址|是|是|

央国企网络岗、运维岗很容易问这个。

---

## 4.10 常见以太网速率

|标准名称|速率|说明|
|---|--:|---|
|10BASE-T|10 Mbps|早期以太网|
|100BASE-TX|100 Mbps|百兆以太网|
|1000BASE-T|1 Gbps|千兆以太网|
|10GBASE-T|10 Gbps|万兆以太网|

### 100BASE-TX 怎么看？

```text
100：速率 100 Mbps
BASE：基带传输
T/TX：双绞线
```

---

## 4.11 央国企考法

### 选择题

> IEEE 802.3 标准定义的是？

答案：

```text
以太网
```

---

> 传统以太网采用的介质访问控制方法是？

答案：

```text
CSMA/CD
```

---

> MAC 地址属于 OSI 哪一层？

答案：

```text
数据链路层
```

---

> 交换机根据什么转发数据帧？

答案：

```text
MAC 地址表
```

---

## 4.12 802.3 记忆总结

```text
802.3 = Ethernet = 以太网 = 有线局域网
```

必背关键词：

```text
MAC 地址
以太网帧
CSMA/CD
交换机
双绞线
全双工
```

---

# 5. IEEE 802.4：Token Bus 令牌总线

## 5.1 它是什么？

**IEEE 802.4 是令牌总线标准。**

它的核心思想是：

```text
谁拿到令牌，谁才能发送数据。
```

---

## 5.2 什么是令牌？

令牌可以理解成一个“发送权限”。

网络中只有拿到令牌的设备才能发送数据。

这样可以避免多个设备同时发送造成冲突。

---

## 5.3 为什么叫令牌总线？

它物理结构类似总线，但逻辑上形成一个令牌传递顺序。

简单理解：

```text
A → B → C → D → A
```

令牌按照固定顺序传递。

---

## 5.4 优点和缺点

### 优点

```text
不会发生冲突
访问有序
适合对实时性有要求的场景
```

### 缺点

```text
实现复杂
维护麻烦
灵活性不如以太网
后来基本被以太网取代
```

---

## 5.5 央国企考法

一般只考对应关系：

> IEEE 802.4 是什么？

答案：

```text
令牌总线 Token Bus
```

可能还会问：

> 令牌总线如何控制发送？

答案：

```text
通过令牌控制，拿到令牌的站点才能发送数据。
```

---

## 5.6 802.4 记忆总结

```text
802.4 = Token Bus = 令牌总线
```

常考程度：

```text
低，了解即可
```

---

# 6. IEEE 802.5：Token Ring 令牌环

## 6.1 它是什么？

**IEEE 802.5 是令牌环标准。**

它和 802.4 很像，也使用令牌机制。

区别是：

```text
802.4 是令牌总线
802.5 是令牌环
```

---

## 6.2 令牌环怎么工作？

设备在逻辑上形成一个环：

```text
A → B → C → D → A
```

令牌在环中依次传递。

谁拿到令牌，谁发送数据。

---

## 6.3 令牌环特点

### 优点

```text
无冲突
传输有序
延迟可预测
```

### 缺点

```text
结构复杂
成本较高
故障影响较大
后来被以太网取代
```

---

## 6.4 和以太网区别

|对比|令牌环 802.5|以太网 802.3|
|---|---|---|
|控制方式|令牌传递|CSMA/CD|
|是否冲突|基本无冲突|早期可能冲突|
|成本|较高|较低|
|现状|基本淘汰|主流|

---

## 6.5 央国企考法

> IEEE 802.5 对应什么？

答案：

```text
Token Ring 令牌环
```

---

> 令牌环中谁可以发送数据？

答案：

```text
获得令牌的站点
```

---

## 6.6 802.5 记忆总结

```text
802.5 = Token Ring = 令牌环
```

常考程度：

```text
低，了解即可
```

---

# 7. IEEE 802.11：WLAN / Wi-Fi 无线局域网

## 7.1 它是什么？

**IEEE 802.11 是无线局域网标准。**

也就是 Wi-Fi 的技术基础。

看到：

```text
IEEE 802.11
```

直接想到：

```text
WLAN / Wi-Fi / 无线局域网
```

---

## 7.2 WLAN 和 Wi-Fi 的关系

|名称|含义|
|---|---|
|WLAN|Wireless LAN，无线局域网|
|Wi-Fi|基于 802.11 的无线网络品牌/认证名称|
|IEEE 802.11|无线局域网技术标准|

简单说：

```text
802.11 是标准
Wi-Fi 是我们日常说法
WLAN 是技术类别
```

---

## 7.3 重点一：AP

AP 全称：

```text
Access Point
无线接入点
```

作用：

> **让无线终端接入有线网络。**

比如：

- 家用无线路由器
    
- 公司吸顶 AP
    
- 校园网 AP
    
- 商场 Wi-Fi AP
    

严格说，家用无线路由器通常包含多个功能：

```text
路由器 + 交换机 + AP + NAT + DHCP
```

其中无线接入功能就是 AP。

---

## 7.4 重点二：SSID

SSID 全称：

```text
Service Set Identifier
服务集标识
```

简单说：

```text
SSID 就是 Wi-Fi 名称。
```

比如：

```text
Goertek-Office
CMCC-Guest
QUST-WiFi
```

---

## 7.5 重点三：BSSID

BSSID 通常对应：

```text
AP 的无线 MAC 地址
```

一个 Wi-Fi 名称 SSID 下，可能有多个 AP。

比如公司里都叫：

```text
Goertek-Office
```

但是每个楼层、每个区域的 AP 有不同 BSSID。

---

## 7.6 重点四：CSMA/CA

无线局域网使用：

```text
CSMA/CA
```

全称：

```text
Carrier Sense Multiple Access with Collision Avoidance
载波监听多路访问 / 冲突避免
```

注意：

```text
以太网 802.3：CSMA/CD，冲突检测
无线网 802.11：CSMA/CA，冲突避免
```

这是高频考点。

---

## 7.7 为什么无线用 CSMA/CA，不用 CSMA/CD？

因为无线环境中，设备很难一边发送一边检测冲突。

原因包括：

- 无线信号容易衰减
    
- 发送信号远强于接收信号
    
- 隐藏终端问题
    
- 无线介质是共享空间
    

所以无线网络更强调：

```text
尽量避免冲突
```

而不是发生后再检测。

---

## 7.8 隐藏终端问题

这是无线网络经典问题。

假设：

```text
A 能听到 AP
C 也能听到 AP
但 A 和 C 互相听不到
```

结构：

```text
A        AP        C
```

A 和 C 都认为信道空闲，于是同时向 AP 发送数据，结果 AP 处发生冲突。

这叫：

```text
隐藏终端问题
```

解决思路之一：

```text
RTS/CTS 机制
```

---

## 7.9 RTS/CTS

RTS：

```text
Request To Send
请求发送
```

CTS：

```text
Clear To Send
允许发送
```

过程：

1. 终端先发 RTS；
    
2. AP 回复 CTS；
    
3. 周围设备听到 CTS 后暂时不发送；
    
4. 发送方再正式发送数据。
    

目的：

```text
减少无线冲突
```

---

## 7.10 重点五：常见 Wi-Fi 标准

|IEEE 标准|常见名称|频段|特点|
|---|---|---|---|
|802.11b|早期 Wi-Fi|2.4GHz|速率低|
|802.11g|早期 Wi-Fi|2.4GHz|比 b 快|
|802.11n|Wi-Fi 4|2.4GHz / 5GHz|支持 MIMO|
|802.11ac|Wi-Fi 5|5GHz|速率更高|
|802.11ax|Wi-Fi 6 / 6E|2.4GHz / 5GHz / 6GHz|高并发、效率高|
|802.11be|Wi-Fi 7|2.4GHz / 5GHz / 6GHz|更高速率、更低延迟|

央国企笔试一般不会细考 Wi-Fi 7，但 802.11 = WLAN 必须记。

---

## 7.11 2.4GHz 和 5GHz 区别

|对比|2.4GHz|5GHz|
|---|---|---|
|覆盖范围|更远|较近|
|穿墙能力|更强|较弱|
|干扰|多|相对少|
|速率|较低|较高|
|常见场景|大范围覆盖|高速接入|

简单记：

```text
2.4G 传得远，速度一般，干扰多
5G 速度快，覆盖近，干扰少
```

注意这里的 5G 指 Wi-Fi 5GHz 频段，不是手机 5G 通信。

---

## 7.12 802.11 和 802.3 对比

|对比|802.3|802.11|
|---|---|---|
|名称|以太网|无线局域网|
|介质|网线 / 光纤|无线电波|
|常见设备|交换机|AP / 无线路由器|
|访问控制|CSMA/CD|CSMA/CA|
|稳定性|更稳定|受环境影响|
|安全风险|相对较低|更容易被监听和干扰|

---

## 7.13 央国企考法

> IEEE 802.11 对应什么？

答案：

```text
无线局域网 / WLAN / Wi-Fi
```

---

> 无线局域网采用的介质访问控制方式是？

答案：

```text
CSMA/CA
```

---

> Wi-Fi 名称叫做什么？

答案：

```text
SSID
```

---

> AP 的作用是什么？

答案：

```text
提供无线接入，使无线终端接入网络
```

---

## 7.14 802.11 记忆总结

```text
802.11 = WLAN = Wi-Fi = 无线局域网
```

必背关键词：

```text
AP
SSID
CSMA/CA
隐藏终端
RTS/CTS
2.4GHz / 5GHz
```

---

# 8. IEEE 802.15：WPAN 无线个人区域网

## 8.1 它是什么？

**IEEE 802.15 是无线个人区域网标准。**

WPAN 全称：

```text
Wireless Personal Area Network
无线个人区域网
```

它主要用于短距离、低功耗通信。

---

## 8.2 常见应用

802.15 相关技术常见于：

- 蓝牙耳机
    
- 蓝牙键盘
    
- 蓝牙鼠标
    
- 智能手环
    
- ZigBee 传感器
    
- 智能家居
    
- 物联网设备
    
- 低功耗无线设备
    

---

## 8.3 802.15 和蓝牙

一般考试中可以这样记：

```text
802.15 = WPAN = 蓝牙相关
```

但更准确地说：

- 802.15 是无线个人区域网标准族；
    
- 蓝牙和 802.15.1 关系较近；
    
- ZigBee 常和 802.15.4 相关。
    

---

## 8.4 802.15.4 和 ZigBee

这个在物联网岗位可能会考。

```text
802.15.4
```

主要用于：

```text
低速率、低功耗无线个人区域网
```

ZigBee 常基于 802.15.4 的物理层和 MAC 层。

特点：

```text
低功耗
低速率
低成本
适合传感器网络
```

---

## 8.5 802.15 和 802.11 对比

|对比|802.11|802.15|
|---|---|---|
|网络类型|WLAN|WPAN|
|代表技术|Wi-Fi|蓝牙、ZigBee|
|覆盖范围|较大|较小|
|速率|较高|一般较低|
|功耗|相对较高|较低|
|典型设备|手机、电脑、AP|耳机、鼠标、传感器|

---

## 8.6 央国企考法

> IEEE 802.15 对应什么？

答案：

```text
无线个人区域网 WPAN
```

---

> 蓝牙、ZigBee 更接近哪个 IEEE 标准族？

答案：

```text
IEEE 802.15
```

---

> ZigBee 常与哪个标准有关？

答案：

```text
IEEE 802.15.4
```

---

## 8.7 802.15 记忆总结

```text
802.15 = WPAN = 蓝牙 / ZigBee / 物联网低功耗通信
```

---

# 9. IEEE 802.16：WiMAX 无线城域网

## 9.1 它是什么？

**IEEE 802.16 是无线城域网标准。**

常和 WiMAX 相关。

WiMAX 全称：

```text
Worldwide Interoperability for Microwave Access
全球微波接入互操作性
```

简单理解：

```text
802.16 是比 Wi-Fi 覆盖范围更大的无线宽带接入技术。
```

---

## 9.2 WLAN、WPAN、WMAN 区别

|类型|中文|IEEE 标准|代表技术|
|---|---|---|---|
|WPAN|无线个人区域网|802.15|蓝牙、ZigBee|
|WLAN|无线局域网|802.11|Wi-Fi|
|WMAN|无线城域网|802.16|WiMAX|

范围从小到大：

```text
WPAN < WLAN < WMAN
```

也就是：

```text
蓝牙 < Wi-Fi < WiMAX
```

---

## 9.3 WiMAX 为什么现在不常见？

WiMAX 曾经被认为可以作为大范围无线宽带接入技术。

但后来：

```text
4G / LTE / 5G 移动通信发展更快
```

所以 WiMAX 没有成为现在的主流。

考试一般只要求知道：

```text
802.16 = WiMAX = 无线城域网
```

---

## 9.4 央国企考法

> IEEE 802.16 对应什么？

答案：

```text
无线城域网 / WiMAX
```

---

> WLAN、WPAN、WMAN 分别对应什么？

答案：

```text
WLAN：802.11，Wi-Fi
WPAN：802.15，蓝牙 / ZigBee
WMAN：802.16，WiMAX
```

---

## 9.5 802.16 记忆总结

```text
802.16 = WMAN = WiMAX = 无线城域网
```

常考程度：

```text
低，了解即可
```

---

# 10. 这些标准按重要程度排序

从央国企考试角度，建议你这样掌握：

|重要程度|标准|必须掌握内容|
|---|---|---|
|极高|802.3|以太网、MAC、CSMA/CD、交换机|
|极高|802.11|Wi-Fi、WLAN、CSMA/CA、AP、SSID|
|高|802.1|VLAN、802.1Q、STP、802.1X|
|中|802.15|WPAN、蓝牙、ZigBee|
|中|802.2|LLC|
|低|802.16|WiMAX、无线城域网|
|低|802.4|令牌总线|
|低|802.5|令牌环|

---

# 11. 最容易出的选择题总结

## 题 1

> IEEE 802.3 标准定义的是？

答案：

```text
以太网
```

---

## 题 2

> IEEE 802.11 标准定义的是？

答案：

```text
无线局域网
```

---

## 题 3

> IEEE 802.15 主要用于？

答案：

```text
无线个人区域网
```

---

## 题 4

> IEEE 802.16 对应的技术是？

答案：

```text
WiMAX / 无线城域网
```

---

## 题 5

> VLAN 标记标准是？

答案：

```text
IEEE 802.1Q
```

---

## 题 6

> 802.1X 主要用于？

答案：

```text
端口访问控制 / 网络接入认证
```

---

## 题 7

> 传统以太网采用的介质访问控制方法是？

答案：

```text
CSMA/CD
```

---

## 题 8

> 无线局域网采用的介质访问控制方法是？

答案：

```text
CSMA/CA
```

---

## 题 9

> 令牌环对应哪个 IEEE 标准？

答案：

```text
IEEE 802.5
```

---

## 题 10

> 令牌总线对应哪个 IEEE 标准？

答案：

```text
IEEE 802.4
```

---

# 12. 最后总表

|标准|名称|核心关键词|央国企掌握程度|
|---|---|---|---|
|IEEE 802|局域网 / 城域网标准族|物理层、数据链路层|必须知道|
|IEEE 802.1|桥接、VLAN、网络管理|802.1Q、VLAN、STP、802.1X|重点|
|IEEE 802.2|LLC 逻辑链路控制|数据链路层上半部分|了解|
|IEEE 802.3|Ethernet 以太网|MAC、交换机、CSMA/CD、网线|必背|
|IEEE 802.4|Token Bus 令牌总线|令牌、总线|了解|
|IEEE 802.5|Token Ring 令牌环|令牌、环形网络|了解|
|IEEE 802.11|WLAN / Wi-Fi|AP、SSID、CSMA/CA、无线局域网|必背|
|IEEE 802.15|WPAN|蓝牙、ZigBee、物联网|常考|
|IEEE 802.16|WiMAX|无线城域网|了解|

---

# 13. 一句话背诵版

```text
IEEE 802 是局域网和城域网标准族；
802.1 管 VLAN、桥接、认证；
802.2 是 LLC；
802.3 是以太网；
802.4 是令牌总线；
802.5 是令牌环；
802.11 是 Wi-Fi；
802.15 是蓝牙、ZigBee 这类个人区域网；
802.16 是 WiMAX 无线城域网。
```

---

# 14. 最好背这个口诀

```text
一桥三以太，
二管 LLC；
四总线，五令环，
十一无线 Wi-Fi；
十五蓝牙物联网，
十六城域 WiMAX。
```

更简单版：

```text
802.3 有线，
802.11 无线，
802.15 蓝牙，
802.16 城域，
802.1 VLAN，
802.2 LLC。
```