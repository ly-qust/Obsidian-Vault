
# 1.
SELECT ... FOR UPDATE：   （锁的是查询命中的记录！）
当前读 + 对命中记录加锁；
锁通常持续到事务 COMMIT 或 ROLLBACK；
阻塞其他事务对同一记录的冲突修改或加锁读取；
普通一致性 SELECT 通常不会被阻塞。

# 2
RC：
每次普通一致性读使用新的快照，
能够看到其他事务新提交的数据。

RR：
同一事务的普通一致性读通常复用第一次建立的快照，
所以重复读取结果保持一致。

FOR UPDATE：
当前读 + 对命中记录加锁，
用于保护“读取、校验、修改”的并发过程。