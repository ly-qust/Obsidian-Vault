这是你的新*仓库*。

写点笔记，[[创建链接]]，或者试一试[导入器](https://help.obsidian.md/Plugins/Importer)插件!

当你准备好了，就将该笔记文件删除，使这个仓库为你所用。
既然每人只有 **2分钟**，老师最想听到的是**“干货”**（即你解决了什么问题，用了什么硬核技术）。

请直接背诵或按照以下这 **四个板块** 进行汇报，保准老师听完直接点头：

---

### 🎤 2分钟汇报脚本（卡表版）

#### 1. 所选题目（10秒）
“老师好！我的题目是《基于 Spring Boot 的高并发政务预约系统》。背景是模拟智慧城市中，市民在短时间内涌入抢号预约政务的场景，核心目标是**解决瞬时高并发下的数据一致性（防止超卖）**。”

#### 2. 开发工具（20秒）
“后端采用 **Spring Boot 3 + MyBatis-Plus**；中间件使用了 **Docker** 部署的 **MySQL 8.0、Redis 7.0 和 RabbitMQ**；接口调试使用了 **Apifox**。目前全套中间件环境已在本地容器化稳定运行。”

#### 3. 设计思路（50秒 - **核心加分项**）
“针对高并发，我设计了**三层防护体系**：
*   **第一层（防超卖）：** 核心业务采用了**数据库乐观锁（Optimistic Locking）**，利用 `Version` 版本号机制确保在高并发冲突时，名额扣减绝对准确。
*   **第二层（抗压）：** 引入 **Redis 缓存预热**，名额预先加载到内存，利用 **Lua 脚本** 进行原子扣减，将大部分请求拦截在数据库之前。
*   **第三层（解耦）：** 使用 **RabbitMQ** 进行异步削峰，用户抢号成功后先返回结果，后台再异步写入数据库，保护数据库不宕机。”

#### 4. 进展情况（40秒）
“目前进度已完成 70%**：
*   已完成 **Docker 容器环境**和**数据库建模**。
*   已跑通**‘查询->预约->生成记录’**的核心链路，并通过 Apifox 验证了**乐观锁版本更新逻辑**。
*   代码中已实现**全局异常捕获**和**参数合法性校验**。
*   **下一步计划**：进行 JMeter 压力测试，并完善 redis缓存预热和RabbitMQ 的消息补偿机制。”

---

### 💡 老师必问的 3 个“灵魂拷问”及话术

如果老师打断你提问，请用这几句话“绝杀”：

*   **问：你这个项目高并发体现在哪？**
    *   **答：** 体现在**非阻塞锁（乐观锁）**的使用和**缓存/消息队列的配合**。我不是靠增加服务器配置，而是通过**异步化和原子操作**来提升单机的 QPS 处理能力。

*   **问：为什么用乐观锁，不用 synchronized？**
    *   **答：** `synchronized` 是本地锁，会阻塞线程，高并发下性能极差。**乐观锁（Version 机制）**在数据库层面控制，不阻塞请求，且能保证分布式环境下的数据安全。

*   **问：现在代码能跑通吗？怎么证明？**
    *   **答：** “可以。我在 Apifox 里测试过，每成功预约一次，数据库里的 **`remained_stock`（库存）会减 1**，而 **`version`（版本号）会加 1**。这就是并发安全生效的最好证明。”

---

### ⏱️ 最后提醒（还有15分钟）：
1.  **打开项目**：确保 IDE（代码）和 Docker 已经运行。
2.  **准备演示**：如果老师让你看一眼，你直接展示那个 **`AppointmentServiceImpl` 的 `reserve` 方法**，指着 `@Version` 和 `updateById` 说这两个词就行。

**祝你面试/汇报大获全胜！加油！**